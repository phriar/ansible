---
# Variables that apply to every host in production

env: production
ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org

dns_servers:
  - 1.1.1.1
  - 8.8.8.8

timezone: UTC

# Loaded from vault — reference vault vars here
# admin_password: "{{ vault_admin_password }}"
