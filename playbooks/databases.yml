---
# Configure database servers.
# Usage: ansible-playbook playbooks/databases.yml

- name: Configure databases
  hosts: databases
  gather_facts: true

  vars_files:
    - "{{ inventory_dir }}/group_vars/all.yml"

  roles:
    - common
    # - database   # add your database role here

  tags: databases
