---
# Lowest-priority defaults — override in vars/main.yml, group_vars, or host_vars.

ops_user: ansible
timezone: UTC
common_packages:
  - curl
  - vim
  - git
  - unzip

sysctl_settings:
  net.ipv4.tcp_syncookies: 1
  vm.swappiness: 10
